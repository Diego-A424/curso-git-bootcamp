# Plantilla Swift

Copia este bloque y adaptalo dentro de `Sources/IOSLaunchLab/main.swift`, debajo del comentario:

```swift
ideas.append(
    AppIdea(
        owner: "Tu Nombre",
        semester: "5to semestre",
        appName: "FocusMate",
        problem: "Los estudiantes pierden el seguimiento de sus entregas y practicas.",
        flagshipFeature: "Un resumen diario con pendientes, prioridad y avance.",
        swiftConcept: "Structs y arrays"
    )
)
```

Reglas:

- No borres aportes de otros.
- No edites el bloque del instructor.
- Cambia solo tu `AppIdea`.
- Ejecuta `swift run` antes de hacer commit.
- Si algo sale mal, corre `git status` antes de intentar arreglarlo.
